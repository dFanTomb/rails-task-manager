
<h1> Task <%= @task.title %></h1>

<p><%= @task.details %></p>

<p><%= link_to 'Back to tasks', tasks_path %></p>
<p><%= link_to 'Delete', delete_task_path(@task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %></p>
<p><%= link_to 'Edit', edit_task_path(@task) %></p>
